#include <stdio.h>

/**
 * main - Entry point
 * counting in base 10
 *
 * Return: always 0 (success)
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
	{
		printf("%i", a);
	}
	printf("\n");
	return (0);
}
