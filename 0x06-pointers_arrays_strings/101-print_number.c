#include "main.h"

/**
 * print_number - function to print integer
 * @n: parameter
 * Return: 0 success
 */

void print_number(int n)
{
	int n = num;

	if (num < 0)
	{
		_putchar(num);
	}
	else if (num == 0)
	{
		_putchar(num);
	}
	else
	{
		_putchar(num);
	}
}
