#include "main.h"
#include <string.h>

/**
 * _strcmp - function to compare strings
 * @s1: string one
 * @s2: string two
 * Return: 1(success)
 */
int _strcmp(char *s1, char *s2)
{
	value = strcmp(s1, s2);
	if (value == 0)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
