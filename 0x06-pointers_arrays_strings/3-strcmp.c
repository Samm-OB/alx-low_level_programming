#include "main.h"
#include <string.h>

/**
 * _strcmp - function to compare strings
 * @s1: string one
 * @s2: string two
 * Return: string
 */
int _strcmp(char *s1, char *s2)
{
	strcmp(s1, s2);
	return (s1);
}
