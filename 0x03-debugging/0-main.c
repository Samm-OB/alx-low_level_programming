#include "main.h"

/**
 * main - Entry point
 * testthe function if the integer is positive, negative or zero
 *
 * Return: always 0 (success)
 */

int main(void)
{
	int a;

	a = 0;
	positive_or_negative(i);

	return (0);
}
