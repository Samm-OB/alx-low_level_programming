#include "main.h"

/**
 * main - test the function if the integer is positive, negative or zero
 *
 * Return: always 0 (success)
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
