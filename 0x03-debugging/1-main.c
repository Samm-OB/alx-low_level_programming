#include "main.c"

/**
 * main - Entry point
 * this should cause an infinite loop
 *
 * Return: 0 (success)
 */

int main(void)
{
	int b;

	printf("Infinite loop incoming :(\n");

	b = 0;

	/**
	 *while(b < 10)
	 *{
	 *	putchar(b);
	 *}
	 */

	printf("Infinite loop avoided! \\o/\n");
	return (0);
}
